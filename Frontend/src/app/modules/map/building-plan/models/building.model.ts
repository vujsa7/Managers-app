export class Building {
    id: number;
    name: string;
    description: string;
    points: string;

    constructor(id: number, name: string, description: string, points: string) {
        this.id = id
        this.name = name
        this.description = description
        this.points = points
    }

}